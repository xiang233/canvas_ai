# ============================================================
# LLM Provider:  azure | openai | auto
#   azure  = 只用 Azure OpenAI
#   openai = 只用 OpenAI 官方
#   auto   = 两个都注册，Azure 优先，缺哪个自动降级（默认）
# ============================================================
LLM_PROVIDER=azure

# Agent 运行参数（eval 扫参用）
AGENT_MODEL_ID=gpt-4.1-mini
AGENT_MAX_STEPS=15
# 0 = 关闭周期性 re-planning。开启需要先在 general_agent.yaml 里补 planning 模板，
# 否则 _generate_planning_step() 会抛 KeyError: 'planning'
AGENT_PLANNING_INTERVAL=0

# ---- Azure OpenAI Config ----
# 注意：AGENT_MODEL_ID 和 AZURE_OPENAI_DEPLOYMENTS 里填的是 **deployment name**，不是模型名
AZURE_OPENAI_ENDPOINT=https://canvas-ai-sam.openai.azure.com/
AZURE_OPENAI_API_KEY=your_azure_key_here
AZURE_OPENAI_API_VERSION=2024-08-01-preview
AZURE_OPENAI_DEPLOYMENTS=gpt-4.1-mini
AZURE_OPENAI_MAX_RETRIES=3
AZURE_OPENAI_TIMEOUT=60

# Open AI Config
OPENAI_API_KEY=your_openai_api_key_here

# Log Config

LOG_LEVEL=INFO

# Canvas Config
# CANVAS_URL 填学校自己的 Canvas 域名，例如 https://wustl.instructure.com

CANVAS_URL=https://your-school.instructure.com
CANVAS_ACCESS_TOKEN=your_canvas_access_token_here

# Canvas API 重试：只对 429/5xx 和网络超时重试，指数退避 + 抖动
# 429 若带 Retry-After 头则优先遵循该值
CANVAS_MAX_RETRIES=3
CANVAS_TIMEOUT=30

# WebSocket Server Config
CANVAS_WS_SECRET=your_websocket_secret_here
CANVAS_WS_TOTP_SECRET=your_totp_secret_here
CANVAS_WS_TOTP_DISABLED=false

WSS_ENABLED=true
WSS_CERTFILE=your_certfile_here
WSS_KEYFILE=your_keyfile_here 
CANVAS_WS_URI=wss://your_domain.com:8765

# Max duration of a session in minutes
SESSION_DURATION=10